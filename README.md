# MenuSmart

MVP responsivo em React + TypeScript + Vite para cardápio digital.

Inclui landing page, cardápio público demonstrativo Burger House, busca, filtros, carrinho, checkout via WhatsApp e dashboard básico. Execute com `npm install` e `npm run dev`.
